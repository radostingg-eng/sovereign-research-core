# Synthetic malformed intake corpus

These deliberately invalid JSON documents exercise refusal and byte-preserving
archive behavior without using profile data. Every identifier and value is
invented.
