# E2E Retest Trigger

This marker exists so the push-triggered Runtime Contract workflow executes a fresh repository validation run on `main`.

It is not evidence of a cognitive E2E run. The host-driven E2E cycle must still produce its own validated cycle receipt according to `E2E_TEST_PROTOCOL.md`.
